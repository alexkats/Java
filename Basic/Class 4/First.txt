внутри класса может быть класс - inner class
снаружи - outer class
inner.this
outer.this

static у outer class ничего не значит
static определяет, доступен внешний this или нет,

abstract - можно не все методы записывать
не можем создать экземпляр абстрактного класса
